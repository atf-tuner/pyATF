Prerequisites:
- setup API token in ~/.pypirc
- update pkginfo when errors on missing/unsupported metadata are raised

1. Make sure there are no uncommitted changes
2. Set version number in pyproject.toml
3. Run publish.sh from repository root